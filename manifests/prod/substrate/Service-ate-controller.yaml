apiVersion: v1
kind: Service
metadata:
  labels:
    app: ate-controller
  name: ate-controller
  namespace: ate-system
spec:
  ports:
    - name: metrics
      port: 8080
      protocol: TCP
      targetPort: metrics
  selector:
    app: ate-controller
